<UserControl x:Class="Willow.Kermit.Shell.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Height="24" Background="DarkGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" BorderBrush="Black" BorderThickness="1" Margin="1" Background="{StaticResource KermitBrush}">
            <TextBlock Name="User" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,0,3,0"/>
        </Border>
        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1" Margin="1" Background="{StaticResource KermitBrush}">
            <TextBlock Name="Message" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,0,3,0"/>
        </Border>
        <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1" Margin="1" Background="{StaticResource KermitBrush}">
            <TextBlock Name="Status" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="3,0,3,0"/>
        </Border>
    </Grid>
</UserControl>
